<!DOCTYPE html>
<html>
<head>
    <title>Saberchat</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="/stylesheets/main.css">
    <!-- link css for navbar. Don't need to include public/javascript/userForm.js since user is already logged in and does not need to access login and register forms.  -->
    <link rel="stylesheet" href="/stylesheets/components/navbar.css">
    <link rel="stylesheet" href="/stylesheets/components/header.css">
    <link rel="stylesheet" href="/stylesheets/components/announcement.css">

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"></script>

    <!-- I'm using my fontawesome kit -->
    <script src="https://kit.fontawesome.com/dccc745c41.js" crossorigin="anonymous"></script>
</head>
<body>
    <!-- include the navbar via ejs -->
    <%- include('../partials/navbar') %>
    <!-- include the backdrop for nav -->
    <%- include('../partials/header') %>
    <%- include('../partials/announcement') %>

    <div class="container mt-5">
        <div class="row">
            <div class="col-12">
                <h1 style="text-align: center; width: 100%">Change Email Or Password</h1>
            </div>
        </div>
        <div class="row mt-3">
            <div class="col-md-8 offset-md-2 col-12">
                <h4 class="mb-3">Change Email</h1>
                <form action="/profiles/change-email?_method=put" method="post">
                    <div class="form-group">
                        <label for="usernameInput">Email</label>
                        <input id="usernameInput" class="form-control" type="text" name="email" value="<%= currentUser.email %>" placeholder="Enter New Email" required>
                    </div>

                    <button class="btn btn-lg btn-primary btn-block">Submit</button>
                </form>
                <h4 class="mt-5 mb-3">Change Password</h4>
                <form action="/profiles/change-password?_method=put" method="post">
                    <div class="form-group">
                        <label for="usernameInput">Old Password</label>
                        <input id="usernameInput" class="form-control" type="password" name="oldPassword" placeholder="Enter Old Password" required>
                    </div>
                    <div class="form-group">
                        <label for="usernameInput">New Password</label>
                        <input id="usernameInput" class="form-control" type="password" name="newPassword" placeholder="Enter New Password" required>
                    </div>

                    <button class="btn btn-lg btn-primary btn-block">Submit</button>
                </form>
                <a style="display: block; margin-top: 1rem;" href="/profiles/<%= currentUser._id %>">Go Back</a>
            </div>
        </div>
    </div>
</body>
</html>

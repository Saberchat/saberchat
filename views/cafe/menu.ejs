<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Saberchat</title>

    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css"
          integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/main.css">
    <link rel="stylesheet" href="/stylesheets/components/header.css">
    <link rel="stylesheet" href="/stylesheets/components/navbar.css">
    <link rel="stylesheet" href="/stylesheets/admin/permission.css">
    <link rel="stylesheet" href="/stylesheets/cafe/menu.css">
    <% if (currentUser) { %>
        <% if (currentUser.darkmode) { %>
            <link rel="stylesheet" href="/stylesheets/Darkmode.css">
        <% } %>
    <% } %>
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"
            integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>
    <script src="https://unpkg.com/@popperjs/core@2" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"
            integrity="sha384-OgVRvuATP1z7JjHLkuOU7Xw704+h835Lr+6QL9UvYjZE3Ipu6Tp75j7Bh/kR0JKI"
            crossorigin="anonymous"></script>

    <script src="https://kit.fontawesome.com/dccc745c41.js" crossorigin="anonymous"></script>

    <script src="/javascript/cafe/upvote.js"></script>
    <script src="/javascript/cafe/search.js"></script>
</head>
<body class="mode">
<%- include('../partials/navbar') %>
<%- include('../partials/header') %>

<div class="container mt-5">
    <div class="row">
        <div class="col-12">
            <div class="jumbotron text-center header">
                <h1><i class="fas fa-bars"></i> No Left Turn Cafe Menu </h1>
                <p>All of our products, with photos and descriptions</p>
                <a class="btn btn-primary" href='/cafe'><i class="fas fa-arrow-left"></i> Back to Cafe</a>
                <a class="btn btn-info" href='/cafe?order=true'><i class="fas fa-plus-circle"></i> Order</a>
            </div>
            <div class="search-filter">
                <i class="fas fa-search search"></i>
                <input id="search-input" oninput="search()" class="form-control search" type="text"
                       placeholder="Search..." title="Type in Product">
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-12">

            <% categories.forEach(category => { %>

                    <li class="list-group-item list-group-item-success category-header cafe"
                        id="<%= category._id %>-header">
                        <div class="d-flex w-100 justify-content-between">
                            <h2 class="mb-1"><%= category.name %></h2>
                            <span><%= category.items.length %> item(s)</span>
                        </div>
                    </li>

                    <% category.items.forEach(item => { %>

                        <% if (item.isAvailable) { %>
                            <% if (frequentItems.toString().includes(item._id.toString())) { %>
                                <span class="list-group-item list-group-item-info menu-item <%= category.name.split(' ').join('') %>  cafe"
                                      id="item-<%= item.id %>">

                            <% } else { %>
                                <span class="list-group-item list-group-item-action menu-item <%= category.name.split(' ').join('') %>  cafe"
                                      id="item-<%= item.id %>">
                            <% } %>


                                  <span class="item-name"><%= item.name %> (<%= item.availableItems %> orders available)</span>
                                  - $<%= `${(item.price * 100).toString().slice(0, (item.price * 100).toString().length - 2)}.${(item.price * 100).toString().slice((item.price * 100).toString().length - 2)}` %>

                                <div title="Upvotes" class="upvotes">

                                    <% if (item.upvotes.includes(currentUser._id)) { %>
                                        <span class="upvoted" id="upvote-<%= item._id %>" onclick="upvote(this)"><i
                                                    class="fas fa-arrow-circle-up"></i></span>

                                    <% } else { %>
                                        <span class="not-upvoted" id="upvote-<%= item._id %>" onclick="upvote(this)"><i
                                                    class="fas fa-arrow-circle-up"></i></span>
                                    <% } %>

                                    <span id="upvoteCount-<%= item._id %>"> <%= item.upvotes.length %></span>
                                </div>

                                <% if (frequentItems.toString().includes(item._id.toString())) { %>
                                    <span class="popular">Frequently Ordered</span>
                                <% } %>

                  <br/><em class="item-description convertible-text"><%= itemDescriptions[item._id.toString()] %></em><br/>
                  <% if (item.imgUrl != '') { %>
                      <img class="item-image" src="<%= item.imgUrl %>"/>
                  <% } else if (["png", "jpg", "jpeg"].includes(item.imageFile.url.split("SaberChat/")[1].split('.')[1].toLowerCase())) { %>
                      <img class="item-image" src="<%= item.imageFile.url %>"/>
                  <% } %>

                </span>
                        <% } %>
                    <% }) %>
                    <br class="category-break" id="<%= category._id %>-break"/>
            <% }) %>
            <br/>
        </div>
    </div>
</div>
</body>
<script src="/javascript/embed.js"></script>
</html>
